function scrollY() {
		var de = document.documentElement;
		return self.pageYOffset || (de & de.scrollTop) || document.body.scrollTop;
	}
	//页面状态对象


window.onload = function() {
//	console.log("window.onlod", this)
	fn.Page.InitView();
	fn.AddBaseEv();
	//进行一次请求查询，并把得到的最新的数据缓存，数据包括BUG表列表和最新的一个BUG。然后进行各种初始化
	//初始化BUG视图：
	//	bindEvent();
	//	var i;
	//	for (i in sendData) {
	//		var id = document.getElementById(i);
	//		id.innerHTML = receptionData[i];
	//	}
}
var Loading= {
	id: "id",
	discoverTime: "数据加载中",
	modityTime: "数据加载中",
	bugLevel: "数据加载中",
	dutyPerson: "数据加载中",
	submitPerson: "数据加载中",
	matterText: "数据加载中",
	modityText: "数据加载中",
	lossText: "数据加载中",
	status:0
}